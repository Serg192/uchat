#include "../inc/client.h"

/*

int main(int argc, char** argv) {

	gtk_init(&argc, &argv);

	//GtkWidget * window = gtk_window_new(GTK_WINDOW_TOPLEVEL);

	//gtk_widget_show_all(window);

	auth_window_t*  a_window = mx_build_auth_window();

	gtk_widget_show(a_window->window);

	gtk_main();

	return 0;
}

*/